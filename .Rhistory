# 3_build_graph_and_metrics.R
source("0_functions.R")
# Read edges/nodes from SQL via RxSqlServerData
edges_rx <- RxSqlServerData(
sqlQuery = "
SELECT f.handle AS [from],
t.handle AS [to]
FROM dbo.Reposted AS r
JOIN dbo.Person AS f ON r.$from_id = f.$node_id
JOIN dbo.Person AS t ON r.$to_id   = t.$node_id;",
connectionString = connStr
)
nodes_rx <- RxSqlServerData(
sqlQuery = "SELECT handle AS name FROM dbo.Person;",
connectionString = connStr
)
rxSetComputeContext(sql_cc)
# Run igraph/statnet inside SQL compute context
centrality_df <- rxExec(
function(edges_data, nodes_data) {
library(igraph)
# Pull data into memory inside SQL compute context
edges_df <- rxImport(edges_data)
nodes_df <- rxImport(nodes_data)
g <- graph_from_data_frame(
d = edges_df,
vertices = nodes_df,
directed = TRUE
)
pr  <- page_rank(g, directed = TRUE)$vector
bet <- betweenness(g, directed = TRUE)
clo <- closeness(g, mode = "out", normalized = FALSE)
eig <- evcent(g, directed = TRUE)$vector
deg <- degree(g, mode = "total" )
core_vals <- coreness(g)
hits <- hits_scores(g, scale = TRUE)
data.frame(
name       = names(pr),
pagerank   = as.numeric(pr),
betweenness = as.numeric(bet),
closeness   = as.numeric(clo),
eigenvector_centrality = as.numeric(eig),
degree = as.numeric(deg),
kcore = as.numeric(core_vals),
hub_score =  as.numeric(hits$hub),
authority_score = as.numeric(hits$authority),
stringsAsFactors = FALSE
)
},
edges_data = edges_rx,
nodes_data = nodes_rx,
execObjects = c("connStr")
)[[1]]
# rxExec returns a list
rxSetComputeContext("localpar")
# Write metrics back into Person table
centrality_sql <- RxSqlServerData(
table = "centrality_tmp",
connectionString = connStr
)
rxDataStep(centrality_df, centrality_sql, overwrite = TRUE)
dbExecute(odbc_con, "
UPDATE p
SET p.pagerank   = c.pagerank,
p.betweenness = c.betweenness,
p.closeness   = c.closeness,
p.eigenvector_centrality = c.eigenvector_centrality,
p.total_degree = c.degree,
p.kcore = c.kcore,
p.hub_score = c.hub_score,
p.authority_score = c.authority_score
FROM dbo.Person AS p
JOIN dbo.centrality_tmp AS c ON p.handle = c.name;
")
dbExecute(odbc_con, "DROP TABLE dbo.centrality_tmp;")
dbExecute(odbc_con, "-- Build in-degree counts
SELECT
$to_id AS to_id,
COUNT(*) AS in_degree
INTO #in_degree_tmp
FROM dbo.Reposted
GROUP BY $to_id;
select * from #in_degree_tmp;
-- Update Person table
UPDATE p
SET p.in_degree = c.in_degree
FROM dbo.Person AS p
JOIN #in_degree_tmp AS c
ON p.$node_id = c.to_id;
-- Clean up
DROP TABLE #in_degree_tmp;")
dbExecute(odbc_con, "-- Build out-degree counts
SELECT
$from_id AS from_id,
COUNT(*) AS out_degree
INTO #out_degree_tmp
FROM dbo.Reposted
GROUP BY $from_id;
-- select * from #out_degree_tmp;
-- Update Person table
UPDATE p
SET p.out_degree = c.out_degree
FROM dbo.Person AS p
JOIN #out_degree_tmp AS c
ON p.$node_id = c.from_id;
-- Clean up
DROP TABLE #out_degree_tmp;")
modularity(g)
communities(g)
karate <- make_graph("Zachary")
wc <- cluster_walktrap(karate)
modularity(wc)
membership(wc)
plot(wc, karate)
wc <- cluster_walktrap(g)
modularity(wc)
plot(wc , g)
modularity(g, wc$membership)
# 3_build_graph_and_metrics.R
source("0_functions.R")
# Read edges/nodes from SQL via RxSqlServerData
edges_rx <- RxSqlServerData(
sqlQuery = "
SELECT f.handle AS [from],
t.handle AS [to]
FROM dbo.Reposted AS r
JOIN dbo.Person AS f ON r.$from_id = f.$node_id
JOIN dbo.Person AS t ON r.$to_id   = t.$node_id;",
connectionString = connStr
)
nodes_rx <- RxSqlServerData(
sqlQuery = "SELECT handle AS name FROM dbo.Person;",
connectionString = connStr
)
rxSetComputeContext(sql_cc)
# Run igraph/statnet inside SQL compute context
centrality_df <- rxExec(
function(edges_data, nodes_data) {
library(igraph)
# Pull data into memory inside SQL compute context
edges_df <- rxImport(edges_data)
nodes_df <- rxImport(nodes_data)
g <- graph_from_data_frame(
d = edges_df,
vertices = nodes_df,
directed = TRUE
)
pr  <- page_rank(g, directed = TRUE)$vector
bet <- betweenness(g, directed = TRUE)
clo <- closeness(g, mode = "out", normalized = FALSE)
eig <- evcent(g, directed = TRUE)$vector
deg <- degree(g, mode = "total" )
core_vals <- coreness(g)
hits <- hits_scores(g, scale = TRUE)
comm <- cluster_walktrap(g)
data.frame(
name       = names(pr),
pagerank   = as.numeric(pr),
betweenness = as.numeric(bet),
closeness   = as.numeric(clo),
eigenvector_centrality = as.numeric(eig),
degree = as.numeric(deg),
kcore = as.numeric(core_vals),
hub_score =  as.numeric(hits$hub),
authority_score = as.numeric(hits$authority),
communtity = comm$membership,
modularity = comm$modularity,
stringsAsFactors = FALSE
)
},
edges_data = edges_rx,
nodes_data = nodes_rx,
execObjects = c("connStr")
)[[1]]
comm <- cluster_walktrap(g)
pr  <- page_rank(g, directed = TRUE)$vector
bet <- betweenness(g, directed = TRUE)
clo <- closeness(g, mode = "out", normalized = FALSE)
eig <- evcent(g, directed = TRUE)$vector
deg <- degree(g, mode = "total" )
core_vals <- coreness(g)
hits <- hits_scores(g, scale = TRUE)
data.frame(
name       = names(pr),
pagerank   = as.numeric(pr),
betweenness = as.numeric(bet),
closeness   = as.numeric(clo),
eigenvector_centrality = as.numeric(eig),
degree = as.numeric(deg),
kcore = as.numeric(core_vals),
hub_score =  as.numeric(hits$hub),
authority_score = as.numeric(hits$authority),
communtity = comm$membership,
modularity = comm$modularity,
stringsAsFactors = FALSE
)
bet <- betweenness(g, directed = TRUE)
detach("package:sna", unload = TRUE)
detach("package:tsna", unload = TRUE)
detach("package:statnet", unload = TRUE)
detach("package:statnet.common", unload = TRUE)
detach("package:stats", unload = TRUE)
library(stats, lib.loc = "C:/Program Files/R/R-4.5.2/library")
detach("package:sna", unload = TRUE)
detach("package:tsna", unload = TRUE)
detach("package:visNetwork", unload = TRUE)
detach("package:statnet.common", unload = TRUE)
pr  <- page_rank(g, directed = TRUE)$vector
bet <- betweenness(g, directed = TRUE)
clo <- closeness(g, mode = "out", normalized = FALSE)
eig <- evcent(g, directed = TRUE)$vector
eig <- eigen_centrality(g, directed = TRUE)$vector
deg <- degree(g, mode = "total" )
core_vals <- coreness(g)
hits <- hits_scores(g, scale = TRUE)
data.frame(
name       = names(pr),
pagerank   = as.numeric(pr),
betweenness = as.numeric(bet),
closeness   = as.numeric(clo),
eigenvector_centrality = as.numeric(eig),
degree = as.numeric(deg),
kcore = as.numeric(core_vals),
hub_score =  as.numeric(hits$hub),
authority_score = as.numeric(hits$authority),
communtity = comm$membership,
modularity = comm$modularity,
stringsAsFactors = FALSE
)
comm_vec <- comm$membership names(comm_vec) <- names(pr) comm_aligned <- comm_vec[names(pr)] comm_aligned[is.na(comm_aligned)] <- 0
comm_vec <- comm$membership
names(comm_vec) <- names(pr)
comm_aligned <- comm_vec[names(pr)]
comm_aligned[is.na(comm_aligned)] <- 0
community = comm_aligned, modularity = modularity_val,
community = comm_aligned,
comm_vec <- comm$membership
names(comm_vec) <- names(pr)
comm_aligned <- comm_vec[names(pr)]
comm_aligned[is.na(comm_aligned)] <- 0
community = comm_aligned,
community = comm_aligned
pr  <- page_rank(g, directed = TRUE)$vector
bet <- betweenness(g, directed = TRUE)
clo <- closeness(g, mode = "out", normalized = FALSE)
eig <- eigen_centrality(g, directed = TRUE)$vector
deg <- degree(g, mode = "total" )
core_vals <- coreness(g)
hits <- hits_scores(g, scale = TRUE)
comm <- cluster_walktrap(g)
comm_vec <- comm$membership
names(comm_vec) <- names(pr)
comm_aligned <- comm_vec[names(pr)]
comm_aligned[is.na(comm_aligned)] <- 0
data.frame(
name       = names(pr),
pagerank   = as.numeric(pr),
betweenness = as.numeric(bet),
closeness   = as.numeric(clo),
eigenvector_centrality = as.numeric(eig),
degree = as.numeric(deg),
kcore = as.numeric(core_vals),
hub_score =  as.numeric(hits$hub),
authority_score = as.numeric(hits$authority),
community = as.numeric(comm_aligned),
modularity = as.numeric(modularity_val),
stringsAsFactors = FALSE
)
modularity_val <- comm$modularity
comm_aligned
comm_vec
str(comm)
source("L:/Source/Repos/BlueSkyNetWorks/misc/metrics computations in sql.R")
# Read edges/nodes from SQL via RxSqlServerData
edges_rx <- RxSqlServerData(
sqlQuery = "
SELECT f.handle AS [from],
t.handle AS [to]
FROM dbo.Reposted AS r
JOIN dbo.Person AS f ON r.$from_id = f.$node_id
JOIN dbo.Person AS t ON r.$to_id   = t.$node_id;",
connectionString = connStr
)
nodes_rx <- RxSqlServerData(
sqlQuery = "SELECT handle AS name FROM dbo.Person;",
connectionString = connStr
)
rxSetComputeContext(sql_cc)
# Run igraph/statnet inside SQL compute context
centrality_df <- rxExec(
function(edges_data, nodes_data) {
library(igraph)
# Pull data into memory inside SQL compute context
edges_df <- rxImport(edges_data)
nodes_df <- rxImport(nodes_data)
g <- graph_from_data_frame(
d = edges_df,
vertices = nodes_df,
directed = TRUE
)
pr  <- page_rank(g, directed = TRUE)$vector
bet <- betweenness(g, directed = TRUE)
clo <- closeness(g, mode = "out", normalized = FALSE)
eig <- eigen_centrality(g, directed = TRUE)$vector
deg <- degree(g, mode = "total" )
core_vals <- coreness(g)
hits <- hits_scores(g, scale = TRUE)
comm <- cluster_walktrap(g)
data.frame(
name       = names(pr),
pagerank   = as.numeric(pr),
betweenness = as.numeric(bet),
closeness   = as.numeric(clo),
eigenvector_centrality = as.numeric(eig),
degree = as.numeric(deg),
kcore = as.numeric(core_vals),
hub_score =  as.numeric(hits$hub),
authority_score = as.numeric(hits$authority),
community = as.numeric(comm$membership),
stringsAsFactors = FALSE
)
},
edges_data = edges_rx,
nodes_data = nodes_rx,
execObjects = c("connStr")
)[[1]]
# rxExec returns a list
rxSetComputeContext("localpar")
# Write metrics back into Person table
centrality_sql <- RxSqlServerData(
table = "centrality_tmp",
connectionString = connStr
)
rxDataStep(centrality_df, centrality_sql, overwrite = TRUE)
dbExecute(odbc_con, "
UPDATE p
SET p.pagerank   = c.pagerank,
p.betweenness = c.betweenness,
p.closeness   = c.closeness,
p.eigenvector_centrality = c.eigenvector_centrality,
p.total_degree = c.degree,
p.kcore = c.kcore,
p.hub_score = c.hub_score,
p.authority_score = c.authority_score,
p.community = c.community
FROM dbo.Person AS p
JOIN dbo.centrality_tmp AS c ON p.handle = c.name;
")
dbExecute(odbc_con, "DROP TABLE dbo.centrality_tmp;")
dbExecute(odbc_con, "-- Build in-degree counts
SELECT
$to_id AS to_id,
COUNT(*) AS in_degree
INTO #in_degree_tmp
FROM dbo.Reposted
GROUP BY $to_id;
select * from #in_degree_tmp;
-- Update Person table
UPDATE p
SET p.in_degree = c.in_degree
FROM dbo.Person AS p
JOIN #in_degree_tmp AS c
ON p.$node_id = c.to_id;
-- Clean up
DROP TABLE #in_degree_tmp;")
dbExecute(odbc_con, "-- Build out-degree counts
SELECT
$from_id AS from_id,
COUNT(*) AS out_degree
INTO #out_degree_tmp
FROM dbo.Reposted
GROUP BY $from_id;
-- select * from #out_degree_tmp;
-- Update Person table
UPDATE p
SET p.out_degree = c.out_degree
FROM dbo.Person AS p
JOIN #out_degree_tmp AS c
ON p.$node_id = c.from_id;
-- Clean up
DROP TABLE #out_degree_tmp;")
# master_pipeline.R
source("0_functions.R")
cat("\n=== STEP 1: Ingest from Bluesky API ===\n")
statnet::update_statnet()
# master_pipeline.R
source("0_functions.R")
cat("\n=== STEP 1: Ingest from Bluesky API ===\n")
source("1_BlueSky_Robust.R")
cat("\n=== STEP 2: Load into SQL & build Person/Reposted ===\n")
source("1a_load network data into SQL.r")
View(edges)
View(nodes)
g <- igraph::graph_from_data_frame(edges, directed = TRUE, vertices =  nodes)
plot(g)
View(g)
View(nodes)
g <- igraph::graph_from_data_frame(d = edges, directed = TRUE, vertices =  nodes)
edge_betweenness(g, directed = FALSE, normalized = TRUE)
igraph::edge_betweenness(g, directed = FALSE, normalized = TRUE)
?igraph::edge_betweenness(g, directed = FALSE, normalized = TRUE)
edge_betweenness(g)
edge_betweenness(g, directed = TRUE)
edge_betweenness(g, e = E(g), directed = TRUE)
E(g)
betweenness(g)
igraph::betweenness(g)
e_bet <- igraph::edge_betweenness(g, directed = TRUE)
str(e_bet)
edges[edge_betweenness] <- e_bet
edges[[edge_betweenness] <- e_bet
edges[[edge_betweenness]] <- e_bet
?mutate
edges |> mutate(edge_betweenness = e_bet)
edges <- edges |> mutate(edge_betweenness = e_bet)
source("0_functions.R")
# Step 1: Build edge list (who reposted whom)
edges <- read_parquet("graphs/speirgorm_edges.parquet")
cat("Edges count:", nrow(edges), "\n")
# Debug: inspect edges
print(head(edges))
# remove NAs
edges[is.na(edges)] <- "no text here"
na_per_column <- colSums(is.na(edges))
print("Number of NAs per column:")
print(na_per_column)
# Step 2: Build node list (unique actors and posts)
nodes <- read_parquet("graphs/speirgorm_nodes.parquet")
cat("Nodes count:", nrow(nodes), "\n")
# Debug: inspect nodes
print(head(nodes))
na_per_column <- colSums(is.na(nodes))
print("Number of NAs per column:")
print(na_per_column)
# Step 3: Build igraph object and plot basic network
g <- graph_from_data_frame(d = edges, vertices = nodes, directed = TRUE)
cat("Graph summary:\n")
print(summary(g))
# --- this code bloc needs to go into the vis file
library(tictoc)
tic()
res <- rxExec(
layout_exec,
edges_data = edges,
nodes_data = nodes,
layout_type = rxElemArg(c(
"drl",
"drl_fast",
"graphopt",
"lgl",
"kk",
"tree"
)),
execObjects = c("connStr", "layout_exec")
)
toc()
# res is a list of 6 data.frames in the same order as layout_type
coords_drl <- res[[1]]
coords_drl_fast <- res[[2]]
coords_graphopt <- res[[3]]
coords_lgl <- res[[4]]
coords_kk <- res[[5]]
coords_tree <- res[[6]]
install.packages("future.apply")
cat("\n[1/4] Computing node-level centrality metrics...\n")
cat("\n=== Step 1: Build the initial graph... ===\n")
# Step 1: Build edge list (who reposted whom)
edges <- read_parquet("graphs/speirgorm_edges.parquet")
cat("Edges count:", nrow(edges), "\n")
# Debug: inspect edges
print(head(edges))
# remove NAs
edges[is.na(edges)] <- "no text here"
na_per_column <- colSums(is.na(edges))
print("Number of NAs per column:")
print(na_per_column)
# Step 2: Build node list (unique actors and posts)
nodes <- read_parquet("graphs/speirgorm_nodes.parquet")
cat("Nodes count:", nrow(nodes), "\n")
# Debug: inspect nodes
print(head(nodes))
na_per_column <- colSums(is.na(nodes))
print("Number of NAs per column:")
print(na_per_column)
# Step 3: Build igraph object and plot basic network
g <- graph_from_data_frame(d = edges, vertices = nodes, directed = TRUE)
cat("Graph summary:\n")
print(summary(g))
cat("\n=== BlueSkyNetWorks: Computing Network Metrics using StatNet ===\n")
# Convert igraph to statnet
library(network)
bluSkynet <- asNetwork(g)
?betweenness
# Betweenness Centrality
betweenness_vals <- sna::betweenness(
bluSkynet,
gmode = "digraph",
diag = FALSE,
ignore.eval = FALSE
)
cat("  ✓ Betweenness centrality computed\n")
# Closeness Centrality (suminvdir accounts for directed graphs)
closeness_vals <- sna::closeness(bluSkynet, cmode = "suminvdir", ignore.eval = FALSE)
cat("  ✓ Closeness centrality computed\n")
# Eigenvector Centrality
eigen_vals <- sna::evcent(bluSkynet, gmode = "digraph", ignore.eval = FALSE)
cat("  ✓ Eigenvector centrality computed\n")
# Degree metrics (already computed but ensuring consistency)
in_degree <- sna::degree(bluSkynet, cmode = "indegree")
out_degree <- sna::degree(bluSkynet, cmode = "outdegree")
total_degree <- in_degree + out_degree
cat("  ✓ Degree metrics (in/out/total) computed\n")
